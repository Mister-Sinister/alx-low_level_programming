#include "main.h"
/**
 * print_rev - prints a string in reverse
 * @s: the pointer to the string to reverse
 */

void print_rev(char *s)
{
	int i;

	for (i = 0; i != '\0'; i++)

	while (i <= 0)
	{
		_putchar(s[i]);
		i--;
	}
}
